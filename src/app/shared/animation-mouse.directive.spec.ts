import { AnimationMouseDirective } from './animation-mouse.directive';

describe('AnimationMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationMouseDirective();
    expect(directive).toBeTruthy();
  });
});
